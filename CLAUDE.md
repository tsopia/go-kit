# Go-Kit 开发指南

详见 [AGENTS.md](./AGENTS.md)

---

# AI 严谨开发协议（强制）

本协议在使用 superpowers 技能时自动生效。所有检查清单必须显式勾选，不得跳过。

## ⚠️ 环境隔离铁律（最高优先级）

**任何代码修改前，必须先确认或创建 worktree 隔离环境。禁止直接在主分支开发。**

### 检查命令（每次会话/任务前执行）

```bash
# 1. 确认当前是否在 worktree 中
git worktree list | grep "$(pwd)" | grep -v "(bare)"

# 2. 如果输出为空 → 说明在主仓库，必须创建 worktree
# 3. 如果输出不为空 → 确认分支名是否符合 feature/xxx 规范
```

### 决策流程

| 当前状态 | 必须执行的操作 |
|---------|---------------|
| 在 master/main 分支 | **立即停止**，创建 worktree：`Skill("superpowers:using-git-worktrees")` |
| 已在 worktree | 确认分支名规范，继续任务 |
| 不确定 | 运行上述检查命令，根据结果判断 |

---

## 全局阅读纪律

### 任何任务开始前必须执行

```markdown
## 前置检查

- [ ] **环境隔离确认**：
  - [ ] 已运行 `git worktree list | grep "$(pwd)"` 确认当前环境
  - [ ] 确认结果：□ 主仓库(master/main)  □ Worktree(可开发)
  - [ ] 如为主仓库，已创建 worktree 并切换过去
- [ ] 已完整阅读相关代码文件（完整文件，非片段）
- [ ] 已完整阅读相关测试文件
- [ ] 已列出代码现状（每个判断附行号）
```

### 置信度标注规范

| 级别 | 定义 | 使用场景 |
|------|------|----------|
| **确定** | 完整阅读源码，行号明确 | 源码明确显示，无歧义 |
| **推测** | 基于部分代码或模式推断 | 只看了部分实现，或依赖假设 |
| **未知** | 需要进一步验证 | 未阅读相关代码 |

**规则**：推测和未知必须转化为确定，或已标注风险并获得用户确认。

---

## Phase 1: Brainstorming（设计阶段）

### 强制输入

- [ ] 完整阅读相关代码文件（完整文件，非片段）
- [ ] 完整阅读相关测试文件
- [ ] 列出至少 2 种替代方案
- [ ] 列出"可能否定本设计的技术约束"

### 输出格式

```markdown
## 现状分析（置信度标注）

- 当前已有 X 功能（file.go:10-22）
  - 置信度：确定（完整阅读）
- 项目使用 Y 作为日志库
  - 置信度：推测（在 z.go 看到 Y 调用，未全局确认）

## 风险评估

- 风险 1：Y 的推测可能错误 → 影响日志输出格式
```

### 退出检查

- [ ] 所有「推测」和「未知」已转化为「确定」或已标注风险
- [ ] 已列出证伪证据（哪些观察会否定本设计）

---

## Phase 2: Writing-plans（计划阶段）

### 文件结构约束

```markdown
## 文件结构

| 文件 | 操作 | 预计行数 | 责任 |
|------|------|----------|------|
| `path/file.go` | 修改 | 当前 30 → 45（+15） | 单一职责描述 |
| `path/file_test.go` | 创建 | 60 | 测试覆盖 |

**约束检查**：
- [ ] 每个 Task 修改 ≤ 50 行
- [ ] 测试代码 : 实现代码 ≥ 1.5 : 1
```

### Task 粒度规范

每个 Task 必须是单一职责单元：

```markdown
### Task N: [具体描述]

**目标**：[一句话]

**文件变更**：
- 修改：`path/file.go:10-25`（预计 +15 行）

**约束检查**：
- [ ] 修改后文件总行数 ≤ 50 行
- [ ] 依赖包已确认存在

**子步骤（TDD，每个 2-5 分钟）**：

- [ ] **Step 1: 写失败测试**
```go
// 完整代码
```

- [ ] **Step 2: 运行测试 → 确认失败**
运行：`go test -v ./path -run TestName`
预期：FAIL（具体错误信息）

- [ ] **Step 3: 写最简实现**
```go
// 完整代码
```
**行数检查**：修改后 XX 行 → 满足 ≤ 50 行约束

- [ ] **Step 4: 运行测试 → 确认通过**
运行：`go test -v ./path -run TestName`
预期：PASS

- [ ] **Step 5: Commit**
```bash
git add path/file.go path/file_test.go
git commit -m "type(scope): description"
```
```

### Plan Review 增强检查点

- [ ] 每个 Task 修改行数 ≤ 50？
- [ ] 测试比例 ≥ 1.5:1？
- [ ] 所有依赖已声明？
- [ ] 每个子步骤有明确的验证命令？

---

## Phase 3: Execution（执行阶段）

### 每个 Task 前置检查（必须输出）

```markdown
## Task N 前置检查

- [ ] 我已完整阅读本 Task 涉及的所有文件
- [ ] 我理解本 Task 的所有边界条件
- [ ] 本次修改预计 ≤ 50 行（超过已拆分）
- [ ] 我知道验证命令：`go test -v ./path`
```

### TDD 强制流程（不可跳过）

```
Step 1: 写测试 → 测试必须失败
Step 2: 运行 → 确认失败（验证失败信息符合预期）
Step 3: 写实现（最简代码）
Step 4: 运行 → 确认通过
Step 5: 重构（如有需要，保持通过）
Step 6: Commit
```

### 代码自检清单（提交前必须输出）

```markdown
## 代码自检

- [ ] **错误处理**：所有 error 都被检查或显式忽略（`_` 需注释原因）
- [ ] **并发安全**：共享状态有锁保护或文档说明
- [ ] **边界覆盖**：所有 if/else 分支都有测试
- [ ] **命名一致**：与项目现有风格一致
- [ ] **无死代码**：删除的代码确实未被引用
```

### 验证闭环（每个 Task 结束必须输出）

```markdown
## 验证报告

- [ ] 编译检查：`go build ./...` → PASS
- [ ] 单元测试：`go test -v ./path` → PASS（X/Y）
- [ ] Lint 检查：`golangci-lint run` → PASS
- [ ] 行数统计：修改 X 行，新增测试 Y 行（比例 Y/X = Z:1）
```

---

## 约束违反处理

如果无法遵守约束：

1. **立即停止执行**
2. 向用户说明：
   - 违反了哪条约束
   - 为什么无法遵守
   - 建议的调整方案
3. **获得明确同意后再继续**

---

## 快速参考卡

| 约束 | 数值 | 违反处理 |
|------|------|----------|
| 单次 Task 修改行数 | ≤ 50 | 拆分为子 Task |
| 测试:实现代码比例 | ≥ 1.5:1 | 补充测试 |
| Task 执行时间 | 2-5 分钟 | 进一步拆分 |
| 文件职责 | 单一 | 拆分为新文件 |
